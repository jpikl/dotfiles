# Windows-specific Git configuration
# Inclusion of this file should be enabled in config.local.
# See config.local.template for more details.

[core]
    autocrlf = true
    fscache = true
    preloadIndex = true
