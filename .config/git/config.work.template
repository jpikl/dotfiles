# Work configuration template
# Copy this file as `config.local` to activate it as the default one.
# Copy this file as `config.work` to activate it only within `Work` directory.

[user]
    # Work credentials
    name = Jan Pikl
    email = example@mail.com

[core]
    # Work SSH key
    sshCommand = ssh -i ~/.ssh/id_work
