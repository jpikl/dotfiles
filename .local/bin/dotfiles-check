#!/usr/bin/env bash

set -euo pipefail

files=(
  ~/.bash_logout
  ~/.bash_profile
  ~/.bashrc
  ~/.profile
  ~/.local/bin/*
  ~/.local/lib/bash/*.sh
)

printf "Checking the following files:\n\n"
printf "  %s\n" "${files[@]}"

shellcheck --exclude=SC1090 "${files[@]}" && printf "\nSUCCESS\n"
