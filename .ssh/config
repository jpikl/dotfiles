# Local overrides
# See ~/.ssh/config.local.template for an example.
Include config.local

# Known hosts
Host github.com gitlab.com bitbucket.org
  User git

# Generic hosts
Host *
  IdentityFile ~/.ssh/id_%h
