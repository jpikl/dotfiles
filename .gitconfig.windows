# Tweaks for Git for Windows.
#
# Add the following to ~/.gitconfig.local
# to include this configuration file.
#
# [include]
#    path = ~/.gitconfig.windows
#

[core]
    autocrlf = true
    fileMode = false
    fscache = true
    trustctime = false
